<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="nav-close"><i class="fa fa-times-circle"></i>
  </div>
  <div class="sidebar-collapse">
    <ul class="nav" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
                    <span>
                      <% if current_user.user_information.file_resources.count==0 %>
                          <img alt="image" class="img-circle" src="/img/profile_small.jpg" style='width:64px;height:64px'/>
                      <% else %>
                          <img alt="image" class="img-circle" src="<%= current_user.user_information.file_resources.last.ffx.url %>" style='width:64px;height:64px'/>
                      <% end %>
                    </span>
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                       <span class="block m-t-xs"><strong class="font-bold"><%= current_user.full_name %></strong></span>
                        </span>
          </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
            <li>
              <a onclick="MAIN_LAYER_WINDOW = layer.open({
                      type: 2,
                      title: '个人信息',
                      shadeClose: true,
                      shade: false,
                      maxmin: true, //开启最大化最小化按钮
                      area: ['800px', '440px'],
                      content: '<%= pre_edit_fdn_user_path(current_user) %>'
                      });">
                <i class="fa fa-edit"></i> 修改个人信息
              </a>
            </li>
            <li class="divider"></li>
            <li><a href="<%= quit_fdn_user_session_path(1) %>" onclick="return confirm('确定退出吗？')">
              <i class="fa fa-sign-out"></i> 退出
            </a>
            </li>
          </ul>
        </div>
      </li>
      <%= raw(Fdn::Menu.build_menu(@current_user).join) %>
    </ul>
  </div>
</nav>